import { createGlobalStyle } from 'styled-components';

export const AppGlobalStyle = createGlobalStyle`
  #__next {
    --color-fg: black;
    --font-size-xl: 1.5rem;
    --spacing-base: 8px;
  }
`;
